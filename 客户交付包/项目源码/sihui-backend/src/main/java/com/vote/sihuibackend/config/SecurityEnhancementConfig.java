package com.vote.sihuibackend.config;

import org.springframework.context.annotation.Configuration;

/**
 * 安全增强配置类
 * 
 * 注意：CORS配置已在SecurityConfig中定义，此配置类保留用于未来的安全增强功能
 */
@Configuration
public class SecurityEnhancementConfig {

        // CORS配置已在SecurityConfig中定义，移除重复定义以避免Bean冲突
        // 此配置类保留用于未来的安全增强功能

}